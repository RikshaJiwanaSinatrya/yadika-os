/** Height of the taskbar in px. Windows must never overlap this area. */
export const TASKBAR_HEIGHT = 48;

/** Smallest size a window can be resized to. */
export const MIN_WINDOW_WIDTH = 320;
export const MIN_WINDOW_HEIGHT = 200;

/** How much of a window must stay inside the viewport while dragging. */
export const DRAG_VISIBLE_MARGIN = 80;

/** Offset between cascaded windows. */
export const WINDOW_CASCADE_OFFSET = 32;

/** Base z-index where window stacking starts. */
export const BASE_Z_INDEX = 10;
